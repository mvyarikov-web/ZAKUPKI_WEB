Постановка

Сейчас система держит общий файловый индекс `_search_index.txt`, общий для всех пользователей. Любое обновление или удаление влияет на всех, а пересоздание индекса требует повторной обработки всей папки. Требуется перейти на изолированное хранение индекса в PostgreSQL, поддерживать инкрементальные обновления и исключить повторное OCR/извлечение текста при повторных загрузках.

Ключевые цели:
- изоляция индекса по `owner_id`;
- хранение чанков и метаданных в БД с переиспользованием неизменённых данных;
- мягкое удаление файлов с возможностью восстановления без переиндексации;
- автоматическое управление квотой и очищением кеша на основе онлайновой оценки ценности хранения;
- понятная UI/UX для статуса индекса, дедупликации и квот.


Спецификация

### 1. Архитектура данных
- Используем существующие таблицы `documents` и `chunks`; все запросы фильтруются по `owner_id`.
- В `documents` добавляем поля:
  ```sql
  is_visible BOOLEAN DEFAULT TRUE,
  deleted_at TIMESTAMP NULL,
  last_accessed_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  access_count INTEGER DEFAULT 0,
  indexing_cost_seconds FLOAT DEFAULT 0.0
  ```
- Переиспользуем `size_bytes`, `uploaded_at`, `sha256`, `storage_url`.
- Создаём служебную таблицу `folder_index_status(owner_id, folder_path, root_hash, file_count, bytes_total, indexed_at, last_scanned_at)` с уникальным индексом `(owner_id, folder_path)`.

### 2. Индексация
1. `POST /build_index` принимает папку пользователя (под `uploads/`).
2. Сканируем пути, размеры, mtime и при необходимости sha256.
3. Вычисляем `root_hash`; если совпал — используем существующие данные.
4. Для каждого файла: неизменённые пропускаем, изменённые заново извлекаем (TXT/PDF/DOC/DOCX/XLS/XLSX + OCR по возможности), режем на чанки (~500 токенов, перекрытие 50), сохраняем в транзакции.
5. Обновляем `folder_index_status` с `indexed_at=NOW()`.

### 3. Поиск
- Поиск идёт только по `chunks`/`documents` с фильтром `owner_id`.
- Полнотекстовый поиск: `ILIKE` или `to_tsvector` (GIN индекс), заголовки игнорируются при построении чанков.
- Семантический поиск опционален (поле `embedding` может быть NULL).
- При пустой БД `/search` инициирует построение индекса, если включён `USE_DB_INDEX=True`.

### 4. Интеграция с загрузкой и удалением
- `/upload` сохраняет файл, сбрасывает актуальность папки (`root_hash`), но не запускает индексацию.
- `/delete/<path>` удаляет файл с диска, помечает запись `is_visible=FALSE`, `deleted_at=NOW()`.
- `/files_json` выводит только `is_visible=TRUE`.
- При повторной загрузке файла с тем же sha256 и `is_visible=FALSE` запись восстанавливается (`is_visible=TRUE`, `deleted_at=NULL`) без переработки.

### 5. Дедупликация и дерево файлов
- Перед сохранением считаем sha256; при наличии совпадения внутри пользователя:
  - совпадающий `storage_url` + `sha256` → пропустить;
  - совпадающий `storage_url`, но другой `sha256` → обновить и переиндексировать;
  - новая пара `owner_id + storage_url` → создать запись и индексацию.
- **При совпадении sha256 у разных пользователей**: копировать чанки вместо повторной обработки:
  ```sql
  INSERT INTO chunks (document_id, owner_id, chunk_idx, text, text_sha256, tokens, embedding, created_at)
  SELECT :new_document_id, :new_owner_id, chunk_idx, text, text_sha256, tokens, embedding, NOW()
  FROM chunks WHERE document_id = :source_document_id;
  ```
- `/files_json` строит дерево по реальным путям без виртуальной папки «Загруженные файлы».

### 6. Жизненный цикл и квоты
- **Мягкое удаление**: данные остаются в БД, на диске освобождается место, при повторной загрузке читаются из кеша.
- **Квоты**:
  - `USER_QUOTA_BYTES` на пользователя (default: 10 GB),
  - `DB_STORAGE_LIMIT_BYTES` глобально (default: 100 GB),
  - при превышении — отказ в загрузке и запуск GC.
- **Оценка приоритета удаления** (вычисляется на лету в SQL):

score = clamp[0..1]((0.4×idx_norm - 0.3×storage_norm + 0.3×usage_norm + 0.3) / 0.7)

где:
idx_norm = min(indexing_cost_seconds / 60, 1)
storage_norm = min(size_bytes / (100×1024×1024), 1)
usage_norm = min(access_count / days_since_upload, 1)

- **GC-алгоритм** (запускается при загрузке и опционально по расписанию):
  - Удаляем невидимые записи старше 30 дней с наименьшим score (батчи по 100).
  - При необходимости удаляем видимые файлы, если uploaded_at > 180 дней И last_accessed_at > 90 дней, сортируя по score ASC (батчи по 50).
  - Удаление каскадное: `ON DELETE CASCADE` для чанков.
- **Пример SQL для GC невидимых файлов**:
  ```sql
  WITH candidates AS (
  SELECT d.id,
         LEAST(d.indexing_cost_seconds / 60.0, 1.0) AS idx_norm,
         LEAST(d.size_bytes / (100.0 * 1024 * 1024), 1.0) AS storage_norm,
         LEAST(d.access_count / GREATEST(EXTRACT(EPOCH FROM (NOW() - d.uploaded_at)) / 86400.0, 1.0), 1.0) AS usage_norm,
         d.deleted_at
  FROM documents d
  WHERE d.owner_id = :user
    AND d.is_visible = FALSE
    AND d.deleted_at < NOW() - INTERVAL '30 days'
), scored AS (
  SELECT id,
         GREATEST(0.0, LEAST(1.0, (0.4*idx_norm - 0.3*storage_norm + 0.3*usage_norm + 0.3) / 0.7)) AS score,
         deleted_at
  FROM candidates
  ), to_remove AS (
    SELECT id FROM scored ORDER BY score ASC, deleted_at ASC LIMIT 100
  )
  DELETE FROM documents WHERE id IN (SELECT id FROM to_remove);
  ```
- **Обновление метрик использования**: при выдаче документа/сниппета инкрементируем `access_count` и обновляем `last_accessed_at`.

### 7. UI и API
- **Статус индекса**: `GET /index_status` → `{status: 'ready'|'stale'|'building'|'empty', file_count, bytes_total, indexed_at, needs_reindex}`.
- **UI индикаторы**: 🟢 готов, 🟡 устарел, 🔴 строится/пуст.
- **Квота**: прогресс-бар `[████████░░] 8.5 / 10 GB`, предупреждение при >80%.
- **Управление кешем**:
  - `GET /storage/quota` — статистика (используемое место, разбивка по score: low <0.3, medium 0.3-0.6, high ≥0.6).
  - `POST /storage/clear_cache` — удаление `is_visible=FALSE` (опционально с фильтром `min_retention_score`).
  - `POST /storage/restore/<id>` — восстановление из кеша.
  - `GET /admin/storage_stats` — глобальная сводка для админов.

#### Административная панель
- **Кнопка на главной странице**: «⚙️ Управление хранилищем» (отображается только для роли `admin`).
- **Страница** `/admin/storage`:
  - **Раздел «Квоты»**:
    - Поля ввода для `USER_QUOTA_BYTES` (GB) и `DB_STORAGE_LIMIT_BYTES` (GB).
    - Кнопка «Применить изменения» → `POST /admin/quota/update`.
  - **Раздел «Статистика БД»**:
    - Общий прогресс-бар БД с цветовой индикацией (зелёный <70%, жёлтый 70-85%, красный >85%).
    - Таблица пользователей: `owner_id`, используемое место, % от квоты, количество файлов (видимых/удалённых), разбивка по retention score (high/medium/low).
    - Сортировка по потреблению (DESC).
  - **Раздел «Управление GC»**:
    - Переключатель режима: `Автоматический` / `Ручной` → `POST /admin/gc/mode`.
    - Для авто-режима: поле ввода cron-расписания (default: `0 2 * * *`).
    - Информация о последнем запуске: дата, режим, удалено файлов/байт.
    - Кнопка «Запустить очистку вручную» → модальное окно с параметрами:
      - Цель: невидимые / видимые / все.
      - Минимальный retention score (0.0-1.0).
      - Максимум файлов.
      - Чекбокс «Предпросмотр (dry run)».
      - Кнопки «Выполнить» / «Отмена».
    - Результаты dry run: таблица кандидатов на удаление (filename, owner, size, score, причина).
  - **Раздел «Экстренная блокировка»**:
    - Переключатель `UPLOADS_DISABLED` (вкл/выкл) → `POST /admin/uploads/toggle`.
    - Поле причины (опционально, отображается пользователям при попытке загрузки).
  - **Раздел «Логи»**:
    - Последние 100 записей из `logs/storage_audit.log` с фильтрацией по типу события.

### 8. Тестирование
- **Индексация новой папки**: `documents` и `chunks` заполняются, поиск работает без `_search_index.txt`.
- **Инкрементальность**: изменяется один файл — переиндексируется только он.
- **Изоляция**: `owner_id` различает пользователей, удаление у одного не влияет на другого.
- **Дедупликация**: повторная загрузка в той же папке → skip/обновление по `sha256`.
- **Копирование чанков**: два пользователя загружают один файл → у второго чанки копируются без OCR.
- **Soft delete**: «Удалить всё» скрывает файлы, повторная загрузка восстанавливает без индексации.
- **GC**: при превышении квоты удаляются файлы с минимальным score, активно используемые остаются.
- **Квота**: при превышении пользователь получает `{error: 'quota_exceeded'}`.
### 9. Документация и конфигурация
- Добавить в README раздел «Архитектура индексации в БД».
- Обновить `config_service.py`:
  ```python
  USE_DB_INDEX = True
  CHUNK_SIZE_TOKENS = 500
  CHUNK_OVERLAP_TOKENS = 50
  AUTO_INDEX_ON_UPLOAD = False
  USER_QUOTA_BYTES = 10 * 1024 * 1024 * 1024  # 10 GB
  DB_STORAGE_LIMIT_BYTES = 100 * 1024 * 1024 * 1024  # 100 GB
  ```
- Зафиксировать служебные индексы:
  ```sql
  CREATE INDEX IF NOT EXISTS idx_documents_owner_visible ON documents(owner_id, is_visible);
  CREATE INDEX IF NOT EXISTS idx_documents_deleted_at ON documents(deleted_at) WHERE is_visible = FALSE;
  CREATE INDEX IF NOT EXISTS idx_documents_usage ON documents(owner_id, last_accessed_at) WHERE is_visible = TRUE;
  CREATE INDEX IF NOT EXISTS idx_folder_index_status_owner_path ON folder_index_status(owner_id, folder_path);
  CREATE INDEX IF NOT EXISTS idx_chunks_text_fts ON chunks USING GIN (to_tsvector('russian', text));
  ```

### 10. Вне объёма
- Обработка ZIP/RAR и других архивов исключена.
- Экспорт/импорт legacy `_search_index.txt` опционален и не входит в минимальный объём.

## План внедрения

- [x] Создать миграцию для `documents` (добавить поля `is_visible`, `deleted_at`, `last_accessed_at`, `access_count`, `indexing_cost_seconds`) и таблицу `folder_index_status`, добавить индексы. ✅ Применена 2025-11-05
- [x] Обновить сервис индексации: расчёт `root_hash`, выборка изменённых файлов, запись чанков в БД с измерением `indexing_cost_seconds`. ✅ Создан db_indexing.py 2025-11-05
- [x] Переписать `/build_index` и `/search` на работу с БД, оставить fallback на `_search_index.txt` при `USE_DB_INDEX=False`. ✅ Обновлён search.py 2025-11-05
- [ ] Дополнить загрузчик файлов расчётом `sha256` и логикой восстановления мягко удалённых записей (`is_visible=FALSE` → `TRUE`).
- [ ] Реализовать дедупликацию при повторной загрузке и копирование чанков между пользователями (`INSERT ... SELECT` при совпадении `sha256`).
- [ ] Добавить GC-планировщик: очистка невидимых файлов и видимых с низким score, SQL с CTE для вычисления score на лету, обновление логов.
- [ ] Обновить UI: статусы индекса (🟢🟡🔴), дерево папок по реальным путям, индикаторы квоты, действия с кешем.
- [ ] Расширить API (`/index_status`, `/storage/quota`, `/storage/clear_cache`, `/storage/restore/<id>`, `/admin/storage_stats`) и покрыть автотестами ключевые сценарии.
- [ ] Создать административную панель `/admin/storage` с управлением квотами, статистикой БД, настройками GC, экстренной блокировкой загрузок и просмотром логов.
- [ ] Добавить кнопку «⚙️ Управление хранилищем» на главную страницу (видна только для роли `admin`), ведущую на `/admin/storage`.
- [ ] Обновить документацию в README и зафиксировать конфигурацию в `config_service.py`.
 Обновить документацию (README: архитектура индексации) и конфигурацию проекта (config_service.py), описать переход на USE_DB_INDEX.
