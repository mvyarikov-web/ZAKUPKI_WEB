# Increment 020 — 2025‑11‑10: Глубокий анализ проекта и обновление документации

## Контекст и цель

Цель инкремента — зафиксировать результаты глубокого аудита проекта после перехода на гибридную модель хранения (файлы в `uploads/` + индекс/метаданные в PostgreSQL), описать стоимость хранения (retention score), выявить места прямого использования файловой системы и дать конкретные рекомендации по оптимизации/рефакторингу. В рамках инкремента также обновлён `README.md` (актуализированы разделы по архитектуре и GC).

## Краткое резюме

- Архитектура хранения — гибридная: исходные файлы хранятся на диске в `uploads/`, а метаданные, чанки и полнотекстовый индекс — в PostgreSQL.
- Поиск выполняется исключительно по БД (таблица `search_index` с tsvector + GIN), без чтения исходных файлов.
- Система GC использует retention score, учитывающий частоту доступа, давность последнего обращения и стоимость индексации; для мягко удалённых документов действуют отдельные правила.
- Обнаружены участки, где служебные данные всё ещё пишутся/читаются из файлов (`index/*.json`, промпты), — рекомендуется миграция в БД.
- `README.md` обновлён: добавлен раздел о модели хранения, уточнена формула GC, расширено описание административных API и «отказа от файлового индекса».

## Архитектура хранения и поиск

### Модель хранения (гибридная)

- Физические файлы: `uploads/` (нужны для извлечения текста и скачивания по запросу)
- База данных (PostgreSQL):
	- `documents` — метаданные файлов (sha256, размер, метрики, флаги видимости)
	- `user_documents` — связи пользователь ↔ документ (многие-ко-многим), мягкое удаление
	- `chunks` — текстовые фрагменты (500 токенов, overlap 50), переиспользуются при дубликатах
	- `search_index` — полнотекстовый индекс (tsvector, GIN, автотриггер обновления)

Поток данных:
1) Загрузка: file.save() → `uploads/`
2) SHA256 → дедупликация в `documents`
3) Извлечение текста из файлов → чанкирование → запись в `chunks`
4) Индексация в `search_index` (tsvector)
5) Поиск — только по БД, без чтения исходников

### Дедупликация

- Уникальность по SHA256 в глобальном пуле `documents`
- Повторная загрузка:
	- если документ видим — переиспользование существующих чанков;
	- если мягко удалён — восстановление видимости;
	- если документ другого пользователя — создаётся связь и/или копия привязок.

## Стоимость хранения и GC (retention score)

Метрики `documents`:
- `access_count` — число обращений;
- `last_accessed_at` — время последнего обращения;
- `indexing_cost_seconds` — длительность индексации;
- `size_bytes`, `created_at` — вспомогательные поля.

Формула для активных документов (`is_visible=TRUE`):

```
score = 0.5 × ln(access_count + 1)
			- 0.3 × days_since_last_access
			+ 0.2 × (indexing_cost_seconds / 60)
```

Специальные случаи для мягко удалённых (`is_visible=FALSE`):

```
days_since_deletion > 30 → score = -100.0  (удалить физически)
иначе                       score = -50.0   (низкий приоритет)
```

Порог по умолчанию: `-10.0` (кандидаты на удаление). Планировщик GC — ежедневно (по умолчанию 03:00), лимит на партию удалений настраиваемый.

## Где используется файловая система (итоги проверки)

Легитимно и необходимо:
- `webapp/routes/files.py` — загрузка: сохранение в `uploads/`.
- `webapp/services/db_indexing.py` + `document_processor/*` — чтение исходников для извлечения текста и расчёта SHA256.
- Скачивание файлов — отдача из `uploads/` с проверкой безопасного пути.

Точки для миграции в БД (избавиться от файловых JSON):
- `webapp/routes/ai_analysis.py` — сохранение промптов, результатов и workspace в `index/*.json` и `PROMPT/_last_prompt.txt`.
- `webapp/routes/search.py` — чтение `index/status.json` для отображения прогресса индексации.
- Конфигурация моделей — `index/models.json` (есть таблица `ai_model_configs`, требуется миграция).

Системные файлы (оставить):
- Логи приложения в `logs/` (ротация), вспомогательные dev‑артефакты.

## Рекомендации и план работ

Критический приоритет:
1) Миграция промптов и результатов AI‑анализа в БД.
	 - Использовать существующую таблицу `prompts`.
	 - Создать `ai_analysis_results`:
		 ```sql
		 CREATE TABLE ai_analysis_results (
			 id SERIAL PRIMARY KEY,
			 user_id INTEGER REFERENCES users(id),
			 prompt_id INTEGER REFERENCES prompts(id),
			 model_id VARCHAR(127),
			 result_text TEXT,
			 metadata JSONB,
			 created_at TIMESTAMP DEFAULT NOW()
		 );
		 ```

2) Убрать `index/status.json` → хранить состояние индексации в БД.
	 - Расширить `folder_index_status` полем `status_data JSONB`.

3) Перенести `index/models.json` в `ai_model_configs` (таблица уже есть).
	 - Подготовить скрипт миграции `scripts/migrate_models_json_to_db.py`.

Средний приоритет:
4) Устранить дублирование формирования результатов поиска.
	 - Вынести форматирование и подсветку сниппетов в сервис `SnippetService`.

5) Пересмотреть необходимость `user_documents` (если нет шаринга — упростить модель; если есть — рассмотреть materialized view для ускорения сложных выборок).

6) Централизовать конфигурацию (chunk size/overlap, GC threshold) в `config_service.py` с поддержкой `.env`.

Низкий приоритет (дальше по дорожной карте):
7) Рассмотреть хранение малых файлов (<10MB) в `documents.blob` и/или вынос крупных в S3/MinIO.

## Что обновлено в README.md в этом инкременте

- Добавлен раздел «Модель хранения данных (гибридная архитектура)».
- Актуализирован раздел по индексации в БД и FTS через `search_index` (tsvector + GIN, автотриггер).
- Уточнена и расширена формула retention score, добавлены специальные случаи.
- Расширен раздел об административных API и конфигурации GC.
- Переписан раздел «Отказ от файлового индекса», удалены упоминания `_search_index.txt` как актуального источника.

## Риски и ограничения

- Миграция служебных JSON (промпты, статус индексации, модели) потребует аккуратного бэкапа и одноразового скрипта переноса.
- Полный отказ от `index/status.json` возможен только после обновления UI на чтение статуса из БД.
- Перенос малых файлов в `blob` увеличит размер БД и может повлиять на бэкап/restore.

## Чек‑лист покрытия требований

- [x] Описана модель хранения и подтверждён гибридный подход.
- [x] Документирована стоимость хранения (retention score) и метрики.
- [x] Проведён аудит использования ФС vs БД, выделены проблемные места.
- [x] Даны конкретные рекомендации (крит./средн./низкий приоритет).
- [x] README.md актуализирован под новую архитектуру и GC.

## Next steps (предлагаемая реализация)

1) Создать миграцию БД для `ai_analysis_results` и поля `folder_index_status.status_data`.
2) Реализовать сохранение и чтение промптов/результатов через БД; отказаться от `index/*.json`.
3) Перенести `models.json` в `ai_model_configs` + скрипт миграции.
4) Вынести форматирование сниппетов в отдельный сервис; покрыть тестами.
5) Подготовить решение (или решение‑макет) по хранению малых файлов в `blob` (опционально).


## План перехода на чистую БД PostgreSQL (полный отказ от ФС)

Цель: запретить использование файловой системы для рабочих данных. Все исходные файлы, метаданные, чанки и индексы хранятся и обрабатываются только в PostgreSQL. Промпты и связанные с ними функции — удаляются.

### Ключевые принципы
- Никаких записей/чтений в `uploads/`, `index/`, `PROMPT/` и т.п. — только БД.
- Исходные файлы сохраняются в `documents.blob (BYTEA)`; размер — в `documents.size_bytes`.
- Извлечение текста и индексация работают по байтовому потоку из БД, без временных файлов.
- Лимит размера БД (квота) соблюдается «на лету» — при добавлении новых файлов выполняется анализ и, при необходимости, автоматически удаляются 30% документов с минимальным retention score.
- Планировщик (APScheduler) не используется для удаления: расчёт стоимости и удаление происходят в момент добавления новых данных (on‑write enforcement). Опционально может остаться админ‑кнопка «ручной prune».

### Изменения схемы БД (DDL)
1) Колонка для хранения файла:
	```sql
	ALTER TABLE documents
	  ADD COLUMN IF NOT EXISTS blob BYTEA,
	  ADD COLUMN IF NOT EXISTS size_bytes BIGINT;

	-- Рекомендуется индекс уникальности на sha256 уже существует:
	-- CREATE UNIQUE INDEX IF NOT EXISTS ux_documents_sha256 ON documents(sha256_hash);
	```
2) Каскадные удаления для целостности:
	```sql
	-- Убедиться, что зависимости удаляются каскадом (или добавить ON DELETE CASCADE)
	-- Пример: ALTER TABLE chunks DROP CONSTRAINT fk_chunks_document;
	--         ALTER TABLE chunks
	--           ADD CONSTRAINT fk_chunks_document
	--           FOREIGN KEY (document_id) REFERENCES documents(id) ON DELETE CASCADE;
	```
3) Настройки/квоты:
	```sql
	-- Справочник/таблица настроек (если её нет)
	CREATE TABLE IF NOT EXISTS app_settings (
	  key TEXT PRIMARY KEY,
	  value TEXT NOT NULL
	);

	-- Лимит размера БД (в байтах), пример: 10 ГБ
	INSERT INTO app_settings(key, value)
	  VALUES ('DB_SIZE_LIMIT_BYTES', '10737418240')
	  ON CONFLICT (key) DO UPDATE SET value = EXCLUDED.value;
	```

Примечание: для быстрого решения ограничение можно считать как сумму `documents.size_bytes` (приближение к реальному размеру БД). Более точная оценка — `pg_database_size(current_database())`, но это общий размер БД (включая индексы и прочее).

### Изменения в коде
1) Загрузка файлов:
	- Вместо `file.save(path)` читать байты и сохранять в `documents.blob` + `size_bytes`.
	- Дедупликация по `sha256_hash` по байтам файла.
	- Отказаться от любых путей/папок; убрать все обращения к `uploads/`.

2) Извлечение текста:
	- Обновить `document_processor/*` и экстракторы, чтобы принимать `bytes`/`BytesIO`, а не путь к файлу.
	- PDF/DOCX/XLSX библиотеки поддерживают поток/байты — использовать это вместо временных файлов.

3) Индексация/чанкирование:
	- Чтение исходника из `documents.blob` → извлечение текста → разбиение на чанки → запись в `chunks` и `search_index` (как сейчас).

4) Скачивание файлов:
	- Возвращать `SendFile`‑эквивалент из `BYTEA` через `BytesIO` с корректными заголовками `Content-Disposition`, без чтения ФС.

5) Миграция промптов в БД:
	- ✅ Промпты хранятся в таблице `prompts` (PostgreSQL).
	- ✅ Эндпоинты работают через `PromptService` без файлового кэша.
	- ❌ Удалён `PROMPT/_last_prompt.txt` и класс `PromptManager`.
	- ✅ UI кнопки работают корректно (сохранение/загрузка из БД).

6) Статусы и телеметрия:
	- Отказаться от `index/status.json`; хранить состояние в БД (`folder_index_status.status_data JSONB`) или формировать по запросу.

### Политика удаления при лимите (без планировщика)
- Лимит: `DB_SIZE_LIMIT_BYTES` берётся из настроек/окружения.
- Событие: при добавлении нового документа (перед фактической записью или сразу после в рамках транзакции) выполняется проверка.
- Проверка: если суммарный объём данных превышает лимит (или свободное место ниже порога), выполняется «prune» — удаление 30% документов с наименьшим retention score.

Скетч SQL для удаления 30% «наименее ценных» (актуализируйте условия фильтрации под вашу модель видимости):
```sql
WITH ranked AS (
  SELECT d.id,
			/* расчёт score должен соответствовать коду GC */
			(
			  0.5 * LN(d.access_count + 1)
			  - 0.3 * EXTRACT(DAY FROM (NOW() - COALESCE(d.last_accessed_at, d.created_at)))
			  + 0.2 * (COALESCE(d.indexing_cost_seconds, 0) / 60.0)
			) AS score,
			ROW_NUMBER() OVER (ORDER BY (
			  0.5 * LN(d.access_count + 1)
			  - 0.3 * EXTRACT(DAY FROM (NOW() - COALESCE(d.last_accessed_at, d.created_at)))
			  + 0.2 * (COALESCE(d.indexing_cost_seconds, 0) / 60.0)
			) ASC) AS rn,
			COUNT(*) OVER () AS total
  FROM documents d
  WHERE d.is_visible = TRUE
)
DELETE FROM documents d
USING ranked r
WHERE d.id = r.id
  AND r.rn <= FLOOR(0.3 * r.total)::BIGINT;  -- 30% от общего числа видимых
```

Примечания:
- Гарантируйте `ON DELETE CASCADE` для `chunks`/`search_index`, чтобы не оставлять сироты.
- Выполняйте «prune» в той же транзакции, что и добавление нового документа, чтобы не выйти за лимит.
- При желании можно исключить «свежие» документы (например, uploaded < 24h) дополнительным условием.

Ответ на вопрос «зачем нужен планировщик»: для данной политики — не нужен. Удаление происходит на лету при записи новых данных. Планировщик можно оставить только как админ‑инструмент по кнопке «очистить сейчас» или для редких обслуживающих задач (вне удаления).

### Этапы внедрения (по шагам)
**Примечание**: Старых пользователей нет, откат на ФС не требуется. Стратегия - прямой переход на чистое БД-хранилище.

**⚠️ КРИТИЧЕСКИ ВАЖНО**: Блок 9 обеспечивает ПОЛНОЕ отключение файловой системы. После его выполнения приложение физически не сможет читать/писать файлы из `uploads/`. Все fallback-пути будут удалены. Это гарантирует работу только с PostgreSQL blob storage.

1) DDL миграции: `documents.blob`, `size_bytes`, каскады, `app_settings`. ✅
2) Сервис загрузки в blob с дедупликацией по SHA256. ✅
3) Экстракторы на работу с `bytes` (extract_text_from_bytes). ✅
4) Индексация из blob вместо файловой системы. ✅
5) Политика «30% prune» при загрузке; покрытие тестами. ✅
6) Миграция промптов в БД (через PromptService, без файлов). ✅
7) Подготовка статусов индексации (колонка status_data в БД). ✅
8) Удаление APScheduler для GC (оставить админ-ручку).
9) **[КРИТИЧЕСКИЙ]** Полное отключение ФС: удаление всех функций работы с файлами, fallback-путей, физическое удаление `uploads/`, удаление `index/status.json`.
10) Удаление флага `PURE_DB_MODE` - режим БД обязательный.
11) Финальная валидация и документация.

### Чеклист (выполнять последовательно)

#### 1. Подготовка схемы
- [x] Добавить колонку `blob BYTEA` в `documents`
- [x] Добавить колонку `size_bytes BIGINT` в `documents` (уже существовала)
- [x] Проверить/создать уникальный индекс на `sha256_hash` (существует через UniqueConstraint)
- [x] Включить/проверить `ON DELETE CASCADE` для `chunks`, `search_index`
- [x] Создать таблицу `app_settings` (если отсутствует)
- [x] Записать значение `DB_SIZE_LIMIT_BYTES`

#### 2. Код загрузки в БД
- [x] Добавить флаг `PURE_DB_MODE` в конфиг
- [x] Реализовать чтение бинарных данных из upload запроса без сохранения на диск
- [x] Считать SHA256 на лету из потока
- [x] Сохранить байты в `documents.blob` (транзакционно)
- [x] Заполнить `size_bytes` и метаданные
- [x] Покрыть тестом загрузку небольшого файла
- [x] Покрыть тестом дедупликацию по SHA256

#### 3. Экстракторы на bytes
- [x] Обновить PDF экстрактор принимать `bytes`/`BytesIO`
- [x] Обновить DOCX экстрактор
- [x] Обновить XLSX экстрактор
- [x] Обновить TXT обработчик (с кодировками) без пути
- [x] Добавить тесты: PDF/DOCX/TXT с кириллицей
- [x] Строгие тесты проверки кириллицы (не мусор, не пустота)

#### 4. Индексация из blob ✅
- [x] Изменить функцию индексации: источник → blob (с fallback на файл)
- [x] Преобразование memoryview в bytes для корректного извлечения текста
- [x] Исправление INSERT запросов: использование правильных колонок (tokens вместо length)
- [x] Добавление created_at с NOW() через template в execute_values
- [x] Модификация функции rebuild_all_documents() для чтения blob из запроса
- [x] Покрыть тестом: индексация нового документа (TXT, DOCX)
- [x] Покрыть тестом: повторная загрузка (дедупликация, отсутствие повторной индексации)
- [x] Функционал перестройки индекса через кнопку (rebuild_all_documents читает из blob)
- [x] Все тесты: 27 passed, 2 skipped (test_extractor_bytesio.py, test_extractor_cyrillic_validation.py, test_blob_storage_service.py, test_indexing_from_blob.py, test_simple_blob.py)

#### 5. Политика prune (30%) ✅
- [x] Реализовать функцию расчёта текущего объёма (сумма `size_bytes`)
- [x] Реализовать выборку документов по `score ASC`
- [x] Реализовать удаление нижних 30% (одна транзакция)
- [x] Интегрировать в `BlobStorageService.save_file_to_db()` - проверка лимита ДО добавления документа
- [x] Покрыть тестом сценарий: превышение лимита → prune → соблюдение лимита
- [x] Тест: `test_prune_policy.py` - 1 passed
- [x] Все связанные тесты: 10 passed, 1 skipped (прune + blob + indexing)

#### 5.5. Админская панель управления прунингом ✅
- [x] Создать HTML-шаблон `templates/admin_settings.html`
	- Тумблер (toggle switch) для AUTO_PRUNE_ENABLED
	- Поле ввода размера БД с выбором единиц (ГБ/МБ)
	- Отображение текущей статистики БД (кол-во документов, размер, % использования)
- [x] Добавить GET `/admin/settings` в `webapp/routes/admin.py`
	- Чтение настроек из app_settings
	- Расчёт статистики через get_storage_stats()
	- Рендер шаблона с данными
- [x] Добавить POST `/admin/settings/prune` в `webapp/routes/admin.py`
	- Валидация формы (auto_prune_enabled, size_limit, size_unit)
	- UPSERT в app_settings (AUTO_PRUNE_ENABLED, DB_SIZE_LIMIT_BYTES)
	- Редирект с flash-сообщением
- [x] Модифицировать `BlobStorageService.check_size_limit_and_prune()`
	- Чтение AUTO_PRUNE_ENABLED из БД
	- Если disabled → skip prune, возврат False при превышении лимита
	- Чтение DB_SIZE_LIMIT_BYTES из БД (приоритет над config)
- [x] Создать миграцию Alembic `9e692bc88cbf_add_default_prune_settings`
	- INSERT AUTO_PRUNE_ENABLED='true' (по умолчанию включён)
	- INSERT DB_SIZE_LIMIT_BYTES='10737418240' (10 ГБ по умолчанию)
	- Применить миграцию: `alembic upgrade head`
- [x] Создать тесты `tests/test_admin_prune_settings.py`
	- Тест дефолтных значений в app_settings
	- Тест UPSERT настроек через SQL
	- Тест чтения флага в BlobStorageService
	- Результат: 3 passed

#### 6. Миграция промптов в БД ✅
**Цель:** Отказаться от файлового хранилища промптов (`PROMPT/_last_prompt.txt`), использовать только PostgreSQL.

**Что сделано:**
- [x] Удалён класс `PromptManager` (обёртка с файловым кэшированием)
- [x] Восстановлены 4 эндпоинта через прямое использование `PromptService`:
  * `POST /ai_analysis/prompts/save` — сохранение промпта в таблицу `prompts`
  * `GET /ai_analysis/prompts/load/<filename>` — загрузка по имени из БД
  * `GET /ai_analysis/prompts/last` — получение последнего промпта пользователя
  * `GET /ai_analysis/prompts/list` — список всех промптов пользователя
- [x] Проверено: `index/analysis_result.json` не используется (legacy файл, можно удалить)
- [x] UI кнопки (💾 Сохранить промпт, 📂 Загрузить промпт) работают через БД

**Инфраструктура:**
- Таблица `prompts` (уже существует): `id`, `user_id`, `filename`, `prompt`, `created_at`
- Сервис `PromptService` в `webapp/services/prompt_service.py`: CRUD операции через SQLAlchemy
- Никаких зависимостей от файловой системы ✅

**Удалено из файловой системы:**
- ❌ `PROMPT/_last_prompt.txt` — больше не используется
- ❌ Любые другие файлы в `PROMPT/` — промпты только в БД

#### 7. Статусы индексации ✅
**Цель:** Подготовить инфраструктуру БД для хранения статусов индексации.

**Что сделано:**
- [x] Добавлена колонка `status_data JSONB` в `folder_index_status` (миграция 209c1dea59f0)
- [x] Колонка уже существовала в схеме — миграция идемпотентна

**Статус:**
- `index/status.json` остаётся как legacy до Блока 9
- Текущий код не пишет в `status.json` (только читает для обратной совместимости)
- Полное отключение `index/status.json` будет в Блоке 9 вместе с удалением всех файловых операций

**Инфраструктура готова:**
- Таблица `folder_index_status` имеет колонку `status_data JSONB`
- Можно сохранять произвольные JSON-данные о прогрессе индексации
- Подготовка к полному отказу от файловой системы (Блок 9)

#### 8. Очистка от планировщика
- [ ] Удалить периодический GC из APScheduler
- [ ] Оставить ручной эндпоинт `POST /admin/prune` (при необходимости)
- [ ] Тест: отсутствие фоновой задачи GC

#### 9. Очистка ФС использования (КРИТИЧЕСКИЙ БЛОК - гарантированный отказ от файлов)
**Цель:** Полностью удалить возможность чтения из файловой системы, оставить только БД

##### 9.1. Удаление функций работы с файлами
- [ ] Удалить функцию `calculate_file_hash(file_path)` из `db_indexing.py`
- [ ] Удалить функцию `calculate_root_hash(folder_path)` из `db_indexing.py`
- [ ] Удалить функцию `build_db_index(db, owner_id, folder_path)` - сканирование папок больше не требуется
- [ ] Удалить функцию `rebuild_all_documents()` - индекс строится только при загрузке в blob
- [ ] Удалить старую функцию `extract_text(file_path)` из `text_extractor.py`
- [ ] Удалить все fallback-блоки `if os.path.exists(file_path):` из индексации

##### 9.2. Удаление эндпоинтов работы с файлами
- [ ] Удалить эндпоинт `/build_index` или переименовать в `/reindex_from_db` (работа только с blob)
- [ ] Модифицировать `/delete/<path>` - удаление только из БД, не из `uploads/`
- [ ] Модифицировать `/delete_folder/<path>` - удаление только связей в БД
- [ ] Модифицировать `/download/<path>` - отдача только из `documents.blob` через BytesIO
- [ ] Удалить `/files_json` или переделать на чтение структуры из `user_documents.user_path`

##### 9.3. Удаление UI элементов работы с файлами
- [ ] Удалить кнопку "Построить индекс" из `templates/index.html` (индекс строится автоматически при загрузке)
- [ ] Удалить отображение файловой структуры из `uploads/` - показывать только из БД
- [ ] Обновить форму загрузки - убрать упоминания "сканирования папок"

##### 9.4. Проверка отсутствия файловых операций
- [ ] `grep -r "uploads/" webapp/ --include="*.py" | grep -v "test"` → должно быть пусто
- [ ] `grep -r "os.path.join.*uploads" webapp/ --include="*.py"` → должно быть пусто
- [ ] `grep -r "os.walk" webapp/ --include="*.py"` → должно быть пусто
- [ ] `grep -r "file_path.*=" webapp/services/db_indexing.py` → минимальные упоминания или удалены
- [ ] `grep -r "extract_text(" webapp/ --include="*.py"` → только `extract_text_from_bytes()`

##### 9.5. Замена логики загрузки/индексации
- [ ] Новый flow: `POST /upload` → `BlobStorageService.save_file_to_db()` → автоиндексация из blob → готово
- [ ] Удалить двухэтапную логику "сохранить файл → построить индекс"
- [ ] Индексация должна происходить сразу при сохранении в blob (в той же транзакции или сразу после)
- [ ] Функция `index_document_to_db()` должна работать ТОЛЬКО с blob, без параметра `file_path`

##### 9.6. Физическое удаление папки uploads/
- [ ] Убедиться что все данные в БД (проверить количество документов с blob IS NOT NULL)
- [ ] Создать бэкап папки `uploads/`: `tar -czf uploads_backup_$(date +%Y%m%d).tar.gz uploads/`
- [ ] Удалить содержимое папки: `rm -rf uploads/*`
- [ ] Обновить `.gitignore` - удалить `uploads/` из игнорирования (папка больше не используется)
- [ ] Добавить в README предупреждение: "Файлы НЕ хранятся на диске, только в БД PostgreSQL"

##### 9.7. Тесты отсутствия файловых операций
- [ ] Тест: попытка вызова `build_db_index()` → функция не существует или выбрасывает исключение
- [ ] Тест: загрузка файла → проверка что `uploads/` остаётся пустой
- [ ] Тест: скачивание файла → проверка что данные берутся из blob, а не из ФС
- [ ] Тест: удаление документа → проверка что файл удаляется только из БД
- [ ] Тест: поиск → проверка что индекс работает без файловой системы

#### 10. Финализация
- [ ] Удалить флаг `PURE_DB_MODE` → сделать режим БД обязательным
- [ ] Обновить README и инкремент документацию
- [ ] Запустить полный тестовый прогон
- [ ] Подготовить release notes

#### 11. Мониторинг (опционально)
- [ ] Добавить метрику количества prune‑операций
- [ ] Добавить метрику среднего retention score
- [ ] Пост‑аудит через 2 недели эксплуатации


### Команды финальной проверки (после Блока 9)

После выполнения Блока 9 запустить следующие команды для проверки отсутствия файловых зависимостей:

```bash
# 1. Проверка отсутствия упоминаний uploads/ в коде (исключая тесты)
grep -r "uploads/" webapp/ --include="*.py" | grep -v test | grep -v "\.pyc"
# Ожидаемый результат: пусто или только комментарии/строки документации

# 2. Проверка отсутствия os.walk (сканирование папок)
grep -r "os\.walk" webapp/ --include="*.py"
# Ожидаемый результат: пусто

# 3. Проверка отсутствия calculate_file_hash
grep -r "calculate_file_hash" webapp/ --include="*.py"
# Ожидаемый результат: пусто (функция удалена)

# 4. Проверка отсутствия build_db_index
grep -r "build_db_index" webapp/ --include="*.py"
# Ожидаемый результат: пусто (функция удалена)

# 5. Проверка использования только extract_text_from_bytes
grep -r "extract_text(" webapp/ --include="*.py" | grep -v "extract_text_from_bytes"
# Ожидаемый результат: пусто (старая функция удалена)

# 6. Проверка что все документы имеют blob
psql $DATABASE_URL -c "SELECT COUNT(*) FROM documents WHERE blob IS NULL;"
# Ожидаемый результат: 0

# 7. Проверка что папка uploads пуста
ls -la uploads/ | wc -l
# Ожидаемый результат: 2 (только . и ..)

# 8. Запуск полного набора тестов
PYTHONPATH=. pytest tests/ -v --tb=short -k "not slow"
# Ожидаемый результат: все тесты проходят без обращений к uploads/
```

### Критерии приёмки и тесты
- **[КРИТИЧНЫЙ]** Отсутствие любых обращений к `uploads/` в рабочем коде (исключая тесты)
- **[КРИТИЧНЫЙ]** Все документы имеют `blob IS NOT NULL`
- **[КРИТИЧНЫЙ]** Папка `uploads/` физически пуста или удалена
- Загрузка/поиск/скачивание полностью работают без ФС.
- При превышении лимита БД новая загрузка инициирует «prune 30%» и завершается успешно.
- Все связи/индексы целы, сирот в `chunks`/`search_index` нет.
- Тесты: happy‑path + крайние случаи (большие файлы, много мелких, пустые/битые документы, параллельные загрузки).
- Попытка вызова удалённых функций (`build_db_index`, `calculate_file_hash`) приводит к `NameError` или `AttributeError`.

### Оптимизации и упрощения (включить в дорожную карту)
- Удалить промпты и AI‑analysis как функционал (если не требуется).
- Упростить схему (рассмотреть отказ от `user_documents`, если шаринг не нужен).
- Централизовать конфиг (chunk size, overlap, лимиты) в `config_service.py`.
- Вынести форматирование сниппетов в отдельный сервис, убрать дубли.
- Добавить materialized view (если останется шаринг) для ускорения выборок.

