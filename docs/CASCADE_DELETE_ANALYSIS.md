# Анализ каскадных удалений при prune операции

**Дата:** 11 ноября 2025 г.  
**Контекст:** Блок 5 инкремента 020 - политика автоматической очистки БД (30% prune)

# Анализ каскадных удалений при prune операции

**Дата:** 11 ноября 2025 г.  
**Контекст:** Блок 5 инкремента 020 - политика автоматической очистки БД (30% prune)

---

## 📋 Краткая сводка (TL;DR)

**Вопрос:** Останутся ли "мёртвые" связи после удаления документов через prune?

**Ответ:** ✅ **НЕТ** — все связи удаляются автоматически через `ON DELETE CASCADE`

**Найденные проблемы:** ❌ **НЕТ** — архитектура корректна

**Граничный случай:** Документ, используемый несколькими пользователями, будет удалён для всех сразу (защита: retention score учитывает `access_count`)

**Действия:** ✅ **Ничего исправлять не нужно**, рекомендуется добавить мониторинг целостности

---

## Вопрос
Останутся ли "мёртвые" связи или значения в таблицах после удаления документов через prune?

## Краткий ответ
**НЕТ** — все связи удаляются автоматически благодаря каскадным удалениям (`ON DELETE CASCADE`).

---

## Детальный анализ

### 1. SQL-запрос удаления (BlobStorageService.check_size_limit_and_prune)

```sql
WITH ranked AS (
    SELECT d.id,
        (
            0.5 * LN(d.access_count + 1)
            - 0.3 * EXTRACT(DAY FROM (NOW() - COALESCE(d.last_accessed_at, d.created_at)))
            + 0.2 * (COALESCE(d.indexing_cost_seconds, 0) / 60.0)
        ) AS score,
        ROW_NUMBER() OVER (ORDER BY score ASC) AS rn,
        COUNT(*) OVER () AS total
    FROM documents d
    WHERE d.blob IS NOT NULL
)
DELETE FROM documents d
USING ranked r
WHERE d.id = r.id
  AND r.rn <= FLOOR(0.3 * r.total)::BIGINT
```

**Что удаляется:** 30% документов с минимальным retention score из таблицы `documents`.

---

### 2. Таблицы с зависимостями от `documents`

#### 2.1. Таблица `chunks` (чанки текста для RAG)

**Связь:**
```python
# models.py, строка 106
document_id = Column(Integer, ForeignKey('documents.id', ondelete='CASCADE'), nullable=False)
```

**Миграция:**
```python
# alembic/versions/14c42e5d4b45_initial_schema_phase1.py, строка 174
sa.ForeignKeyConstraint(['document_id'], ['documents.id'], ondelete='CASCADE')
```

**✅ РЕЗУЛЬТАТ:** При удалении документа **ВСЕ** его чанки удаляются автоматически.

---

#### 2.2. Таблица `user_documents` (связь пользователей с документами)

**Связь:**
```python
# models.py, строка 68
document_id = Column(Integer, ForeignKey('documents.id', ondelete='CASCADE'), nullable=False)
```

**Миграция:**
```python
# alembic/versions/3da82da71390_add_user_documents_table.py, строка 32
sa.ForeignKeyConstraint(['document_id'], ['documents.id'], ondelete='CASCADE')
```

**✅ РЕЗУЛЬТАТ:** При удалении документа **ВСЕ** связи `user_documents` удаляются автоматически.

---

#### 2.3. Таблица `search_index` (полнотекстовый индекс)

**Связь:**
```python
# models.py, строка 566
document_id = Column(Integer, ForeignKey('documents.id', ondelete='CASCADE'), nullable=False)
```

**Миграция:**
```python
# alembic/versions/5ffc684e6693_add_search_index_table.py, строка 32
sa.Column('document_id', sa.Integer(), sa.ForeignKey('documents.id', ondelete='CASCADE'), nullable=False)
```

**✅ РЕЗУЛЬТАТ:** При удалении документа **ВСЕ** записи в `search_index` удаляются автоматически.

---

### 3. Полный граф каскадных удалений

```
documents (DELETE 30%)
    ├── chunks [ON DELETE CASCADE] → удаляется автоматически
    │   └── embeddings (vector колонка) → удаляется вместе с чанком
    │
    ├── user_documents [ON DELETE CASCADE] → удаляется автоматически
    │   └── связи пользователь ↔ документ
    │
    └── search_index [ON DELETE CASCADE] → удаляется автоматически
        ├── content (Text) → удаляется вместе с записью
        ├── search_vector (tsvector) → удаляется вместе с записью
        └── metadata_json (JSONB) → удаляется вместе с записью
```

---

### 4. Проверка наличия "мёртвых" связей

#### Команды для проверки целостности после prune:

```sql
-- 1. Проверка "сирот" в chunks (не должно быть)
SELECT COUNT(*) FROM chunks c
WHERE NOT EXISTS (SELECT 1 FROM documents d WHERE d.id = c.document_id);
-- Ожидаемый результат: 0

-- 2. Проверка "сирот" в user_documents (не должно быть)
SELECT COUNT(*) FROM user_documents ud
WHERE NOT EXISTS (SELECT 1 FROM documents d WHERE d.id = ud.document_id);
-- Ожидаемый результат: 0

-- 3. Проверка "сирот" в search_index (не должно быть)
SELECT COUNT(*) FROM search_index si
WHERE NOT EXISTS (SELECT 1 FROM documents d WHERE d.id = si.document_id);
-- Ожидаемый результат: 0

-- 4. Проверка что все документы с blob имеют чанки
SELECT d.id, d.sha256, COUNT(c.id) as chunk_count
FROM documents d
LEFT JOIN chunks c ON d.id = c.document_id
WHERE d.blob IS NOT NULL
GROUP BY d.id, d.sha256
HAVING COUNT(c.id) = 0;
-- Ожидаемый результат: 0 строк (если все документы проиндексированы)
```

---

### 5. Потенциальные проблемы (ОТСУТСТВУЮТ)

#### ❌ Проблема: "Мёртвые" chunks без документа
**Статус:** НЕ ВОЗНИКНЕТ  
**Причина:** `ForeignKey('documents.id', ondelete='CASCADE')` в модели `Chunk`

#### ❌ Проблема: "Мёртвые" user_documents без документа
**Статус:** НЕ ВОЗНИКНЕТ  
**Причина:** `ForeignKey('documents.id', ondelete='CASCADE')` в модели `UserDocument`

#### ❌ Проблема: "Мёртвые" search_index без документа
**Статус:** НЕ ВОЗНИКНЕТ  
**Причина:** `ForeignKey('documents.id', ondelete='CASCADE')` в модели `SearchIndex`

#### ❌ Проблема: "Мёртвые" embeddings без чанков
**Статус:** НЕ ВОЗНИКНЕТ  
**Причина:** `embedding` — это колонка в таблице `chunks`, удаляется вместе со строкой

---

### 6. Архитектура мягкого удаления (важное уточнение)

**✅ ВАЖНО:** Мягкое удаление реализовано НЕ в таблице `documents`, а в таблице `user_documents`!

**Схема мягкого удаления:**
```python
# UserDocument модель (строка 68 models.py)
is_soft_deleted = Column(Boolean, default=False, nullable=False)
```

**Логика работы:**
1. Таблица `documents` — глобальное хранилище (независимо от пользователей)
2. Таблица `user_documents` — связи пользователь ↔ документ с флагом `is_soft_deleted`
3. При "удалении файла пользователем" → ставится `is_soft_deleted = TRUE` в `user_documents`
4. **Физический документ** в `documents` остаётся нетронутым (может использоваться другими пользователями)

**Текущий prune-запрос:**
```sql
WHERE d.blob IS NOT NULL
```

**Корректность:** ✅ КОРРЕКТНО для данной архитектуры

**Пояснение:**
- Prune удаляет глобальные документы из пула `documents`
- Если документ удалён — автоматически удаляются ВСЕ связи в `user_documents` (CASCADE)
- Флаг `is_soft_deleted` в `user_documents` не влияет на prune глобальных документов
- Это правильное поведение: мягко удалённые пользователем файлы всё равно занимают место

**Вывод:** Фильтр `is_visible` НЕ нужен, т.к. такой колонки нет в `documents`.

---

### 6.1. Сценарий: Документ используется несколькими пользователями

**Пример:**
1. Пользователь A загружает файл → создаётся `documents[id=1]` и `user_documents[user=A, doc=1]`
2. Пользователь B загружает тот же файл (дедупликация по SHA256) → создаётся только `user_documents[user=B, doc=1]`
3. Пользователь A мягко удаляет файл → `user_documents[user=A, doc=1].is_soft_deleted = TRUE`
4. Prune запускается и удаляет `documents[id=1]` (низкий score)

**Результат каскада:**
```
documents[id=1] удаляется
    ├── user_documents[user=A, doc=1] → удаляется (CASCADE)
    ├── user_documents[user=B, doc=1] → удаляется (CASCADE) ⚠️
    ├── chunks[doc=1, *] → все чанки удаляются (CASCADE)
    └── search_index[doc=1, *] → все индексы удаляются (CASCADE)
```

**⚠️ ПОСЛЕДСТВИЕ:** Пользователь B **ПОТЕРЯЕТ** доступ к файлу, хотя сам его не удалял!

**Это нормально?**
- ✅ **ДА**, если цель — освободить место в БД (физическое удаление)
- ❌ **НЕТ**, если пользователь B активно использует файл (высокий `access_count`)

**Защита:**
- Retention score учитывает `access_count` — часто используемые файлы защищены
- Если пользователь B часто обращается к документу, его score будет высоким
- Prune удаляет **30% с минимальным score**, т.е. "забытые" файлы

**Рекомендация:** Добавить метрику в админку — количество документов, используемых несколькими пользователями.

---

### 7. Рекомендации по мониторингу

#### Периодическая проверка целостности (cron/celery):

```python
def check_referential_integrity(db):
    """Проверка отсутствия мёртвых связей после prune."""
    
    orphan_chunks = db.execute(text("""
        SELECT COUNT(*) FROM chunks c
        WHERE NOT EXISTS (SELECT 1 FROM documents d WHERE d.id = c.document_id)
    """)).scalar()
    
    orphan_user_docs = db.execute(text("""
        SELECT COUNT(*) FROM user_documents ud
        WHERE NOT EXISTS (SELECT 1 FROM documents d WHERE d.id = ud.document_id)
    """)).scalar()
    
    orphan_search_index = db.execute(text("""
        SELECT COUNT(*) FROM search_index si
        WHERE NOT EXISTS (SELECT 1 FROM documents d WHERE d.id = si.document_id)
    """)).scalar()
    
    if orphan_chunks > 0 or orphan_user_docs > 0 or orphan_search_index > 0:
        raise IntegrityError(
            f"Обнаружены мёртвые связи: chunks={orphan_chunks}, "
            f"user_documents={orphan_user_docs}, search_index={orphan_search_index}"
        )
    
    return True
```

#### Логирование prune операций:

```python
# В BlobStorageService.check_size_limit_and_prune()
deleted_count = db.execute(text("""
    WITH ranked AS (...)
    DELETE FROM documents d USING ranked r
    WHERE d.id = r.id AND r.rn <= FLOOR(0.3 * r.total)::BIGINT
    RETURNING d.id
""")).rowcount

app.logger.info(f"Prune: удалено {deleted_count} документов (30% с минимальным score)")
```

---

## Итоговые выводы

### ✅ Каскадные удаления работают корректно

Все зависимые таблицы имеют `ON DELETE CASCADE`:
- ✅ `chunks` удаляются вместе с документом
- ✅ `user_documents` удаляются вместе с документом (включая мягко удалённые)
- ✅ `search_index` удаляется вместе с документом

### ✅ Мягкое удаление работает правильно

- Мягкое удаление реализовано через `user_documents.is_soft_deleted`
- Глобальные документы в `documents` не имеют флага видимости
- Prune удаляет документы независимо от мягкого удаления пользователей
- Это корректное поведение: даже "скрытые" файлы занимают место в БД

### 📊 Рекомендации

1. **✅ Ничего исправлять не нужно** — архитектура правильная
2. **Желательно:** Добавить проверку целостности после prune (см. раздел 7)
3. **Опционально:** Логировать количество удалённых документов
4. **Мониторинг:** Периодически запускать SQL-проверки на "сироты"
5. **📊 Метрика для админки:** Добавить отображение:
   - Количество документов, используемых несколькими пользователями
   - Количество мягко удалённых связей (`is_soft_deleted=TRUE`)
   - Топ документов с максимальным `access_count` (защищены от prune)

### 🔍 Граничные случаи

#### Случай 1: Документ используется несколькими пользователями
- **Риск:** Prune удалит документ для всех пользователей одновременно
- **Защита:** Retention score учитывает суммарный `access_count`
- **Решение:** Текущая формула корректна, т.к. популярные файлы имеют высокий score

#### Случай 2: Пользователь мягко удалил файл, но он часто используется другими
- **Поведение:** Файл будет защищён от prune благодаря высокому `access_count`
- **Корректность:** ✅ Правильное поведение (приоритет физического хранения)

#### Случай 3: Все пользователи мягко удалили файл
- **Поведение:** Документ имеет низкий score (нет обращений) → будет удалён prune
- **Корректность:** ✅ Правильное поведение (освобождение места)

#### Случай 4: Новый документ загружен, но БД переполнена
- **Поведение:** Prune удаляет 30% старых документов → место освобождается → новый файл сохраняется
- **Корректность:** ✅ Правильное поведение (on-write enforcement)

---

## Ссылки на код

- **Модели:** `webapp/db/models.py` (строки 35-120, 560-585)
- **Prune логика:** `webapp/services/blob_storage_service.py` (строки 164-240)
- **Миграция chunks:** `alembic/versions/14c42e5d4b45_initial_schema_phase1.py` (строка 174)
- **Миграция user_documents:** `alembic/versions/3da82da71390_add_user_documents_table.py` (строка 32)
- **Миграция search_index:** `alembic/versions/5ffc684e6693_add_search_index_table.py` (строка 32)
