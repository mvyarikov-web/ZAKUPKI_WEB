# Улучшения UX оптимизатора и предупреждений (01.11.2025)

## Обзор изменений

Три улучшения пользовательского опыта в оптимизаторе текста и предупреждениях о битых символах.

## 1. Переименование кнопки "Применить правила"

### Было:
```
⚡ Применить правила
```
Серая кнопка (#6c757d)

### Стало:
```
🗑️ Удалить выделенное
```
Красная кнопка (#dc3545) с белым текстом

### Причина:
- Название "Применить правила" было неинтуитивным
- Пользователь фактически удаляет выделенные жёлтым фрагменты
- Красный цвет ассоциируется с удалением

### Изменения:
- `templates/index.html` - изменён текст и стиль кнопки

---

## 2. Исправление прыжка экрана при удалении

### Проблема:
При нажатии кнопки "Удалить выделенное" экран прыгал в начало текста, теряя текущую позицию просмотра.

### Решение:
Добавлено сохранение и восстановление позиции скролла:

```javascript
// Сохраняем позицию перед обработкой
const scrollPosition = optimizeTextPreview.scrollTop;

// ... выполняем оптимизацию ...

// Восстанавливаем позицию после рендеринга
setTimeout(() => {
    optimizeTextPreview.scrollTop = scrollPosition;
}, 0);
```

### Почему `setTimeout(..., 0)`?
- Рендеринг HTML (`renderHighlightedText`) выполняется синхронно
- Браузеру нужен tick event loop для обновления DOM
- `setTimeout` гарантирует, что восстановление произойдёт после полного рендеринга

### Изменения:
- `static/js/text-optimizer.js` - сохранение/восстановление `scrollTop`

---

## 3. Статистика в предупреждении о битых символах

### Было:
```
⚠️ Много битых символов, рекомендуется оптимизация!
```

### Стало:
```
⚠️ Много битых символов, рекомендуется оптимизация! (15,234 из 99,846 символов, 15.3%)
```

### Что добавлено:
- **Количество битых символов**: точное число с разделителями тысяч
- **Общее количество символов**: для контекста
- **Процент**: доля битых символов от общего количества

### Технические детали:

#### Новая функция `countMojibakeChars()`
```javascript
function countMojibakeChars(text) {
    const mojibakeChars = '–∂—ë–a—ã–μ–æ–ø...';
    let count = 0;
    
    for (let i = 0; i < text.length; i++) {
        if (mojibakeChars.includes(text[i])) {
            count++;
        }
    }
    
    return count;
}
```

Простой и быстрый подсчёт через перебор символов.

#### Обновлённое предупреждение:
```javascript
if (hasMojibake) {
    const mojibakeCount = countMojibakeChars(fullText);
    const mojibakePercent = totalChars > 0 
        ? ((mojibakeCount / totalChars) * 100).toFixed(1) 
        : 0;
    
    ragMetrics.innerHTML = info + 
        `⚠️ ... (${mojibakeCount.toLocaleString('ru-RU')} из ${totalChars.toLocaleString('ru-RU')} символов, ${mojibakePercent}%)`;
}
```

### Примеры вывода:

**Небольшое количество битых символов:**
```
⚠️ Много битых символов, рекомендуется оптимизация! (124 из 5,678 символов, 2.2%)
```

**Большое количество:**
```
⚠️ Много битых символов, рекомендуется оптимизация! (15,234 из 99,846 символов, 15.3%)
```

**Критическое:**
```
⚠️ Много битых символов, рекомендуется оптимизация! (45,678 из 50,000 символов, 91.4%)
```

### Изменения:
- `static/js/rag-analysis.js`:
  - Добавлена функция `countMojibakeChars()`
  - Обновлена логика формирования предупреждения с статистикой
  - Добавлен флаг `g` в regex паттернах для корректного подсчёта

---

## Итоговые улучшения UX

### Кнопка "Удалить выделенное"
✅ Интуитивно понятное название
✅ Красный цвет ассоциируется с действием удаления
✅ Иконка 🗑️ усиливает восприятие

### Сохранение позиции скролла
✅ Пользователь остаётся на том же месте в тексте
✅ Не теряется контекст при редактировании
✅ Комфортная работа с длинными документами

### Статистика битых символов
✅ Конкретные цифры вместо абстрактного "много"
✅ Понимание масштаба проблемы (2% vs 90%)
✅ Мотивация к оптимизации при высоком проценте

## Изменённые файлы

1. `templates/index.html`:
   - Кнопка: текст "🗑️ Удалить выделенное", красный цвет

2. `static/js/text-optimizer.js`:
   - Комментарий обновлён: "Удаление выделенного текста"
   - Сохранение `scrollPosition` перед обработкой
   - Восстановление `scrollPosition` после рендеринга

3. `static/js/rag-analysis.js`:
   - Флаг `g` в regex паттернах `detectMojibake()`
   - Новая функция `countMojibakeChars()`
   - Статистика в предупреждении: `(count из total, percent%)`

## Тестирование

### Тест 1: Кнопка удаления
1. Открыть оптимизатор с текстом
2. Проверить: кнопка красная с иконкой 🗑️
3. Кликнуть → жёлтые фрагменты должны удалиться

### Тест 2: Позиция скролла
1. Открыть оптимизатор с длинным текстом
2. Прокрутить вниз (например, к середине)
3. Нажать "🗑️ Удалить выделенное"
4. Проверить: экран остался на том же месте (не прыгнул в начало)

### Тест 3: Статистика битых символов
1. Вставить текст с битыми символами в AI анализ
2. Проверить предупреждение содержит:
   - Количество битых символов
   - Общее количество
   - Процент
3. Формат: `(15,234 из 99,846 символов, 15.3%)`
