# 🧪 Инструкция по ручному тестированию фантомной подсветки

## Проблема
Фантомная жёлтая подсветка появляется в файлах без активного поиска.

## Исправления (коммит b5ff96b + новый)
1. Добавлена функция `clearHighlights()`
2. Добавлен обработчик `input` на поле поиска
3. Вызовы clearHighlights в 3 точках

---

## 🔬 Тест-сценарий (выполнить в браузере)

### Подготовка:
1. Открыть http://127.0.0.1:8081
2. Открыть DevTools (F12 или Cmd+Option+I)
3. Перейти на вкладку Console

---

### ✅ ТЕСТ 1: Проверка при загрузке страницы

**Шаги:**
1. Обновить страницу (F5 или Cmd+R)
2. Дождаться полной загрузки
3. В консоли выполнить:
```javascript
document.querySelectorAll('.highlight').length
```

**Ожидаемый результат:** `0`  
**Если не 0:** Подсветка осталась от предыдущего поиска ❌

---

### ✅ ТЕСТ 2: Проверка появления подсветки при поиске

**Шаги:**
1. В поле поиска ввести: `договор`
2. Нажать кнопку "Поиск" (или Enter)
3. Дождаться результатов (несколько секунд)
4. В консоли выполнить:
```javascript
document.querySelectorAll('.highlight').length
```

**Ожидаемый результат:** Число > 0 (например, 15-30)  
**Если 0:** Подсветка не работает ❌

**Дополнительная проверка:**
```javascript
// Проверить что подсветка есть в сниппетах
document.querySelectorAll('.context-snippet .highlight').length
```

---

### ✅ ТЕСТ 3: Проверка исчезновения подсветки при очистке поля

**Шаги:**
1. После выполнения поиска (Тест 2)
2. Очистить поле поиска (удалить весь текст вручную)
3. Подождать 0.5 сек
4. В консоли выполнить:
```javascript
document.querySelectorAll('.highlight').length
```

**Ожидаемый результат:** `0`  
**Если не 0:** Обработчик input не срабатывает ❌

---

### ✅ ТЕСТ 4: Проверка при вводе нового запроса

**Шаги:**
1. Ввести в поиск: `договор`
2. Нажать "Поиск"
3. Дождаться результатов
4. Очистить поле и ввести: `цена`
5. НЕ нажимая "Поиск", проверить в консоли:
```javascript
document.querySelectorAll('.highlight').length
```

**Ожидаемый результат:** `0` (старая подсветка удалена)  
**Затем:** Нажать "Поиск" и убедиться что появилась новая подсветка

---

### ✅ ТЕСТ 5: Проверка функции clearHighlights

**Шаги:**
1. Выполнить поиск (любой запрос)
2. В консоли выполнить:
```javascript
// Проверить что функция доступна
typeof clearHighlights
// Должно быть: "function"

// Подсчитать highlights ДО очистки
document.querySelectorAll('.highlight').length
// Должно быть > 0

// Вызвать очистку
clearHighlights()

// Подсчитать highlights ПОСЛЕ очистки
document.querySelectorAll('.highlight').length
// Должно быть: 0
```

---

### ✅ ТЕСТ 6: Проверка сохранения состояния после перезагрузки

**Шаги:**
1. Выполнить поиск: `договор`
2. Дождаться результатов с подсветкой
3. Обновить страницу (F5)
4. В консоли выполнить:
```javascript
document.querySelectorAll('.highlight').length
```

**Ожидаемый результат:** `0` (подсветка не сохранилась)  
**Если не 0:** Проблема с очисткой при загрузке ❌

---

## 📊 Отладка (если тесты не проходят)

### Проверка 1: Обработчик input
```javascript
// В консоли:
var input = document.getElementById('searchInput');
getEventListeners(input).input
// Должен показать массив с обработчиком
```

### Проверка 2: Вызовы clearHighlights
```javascript
// Добавить логирование:
var originalClear = clearHighlights;
clearHighlights = function() {
    console.log('🧹 clearHighlights вызвана!', new Error().stack);
    return originalClear.apply(this, arguments);
}

// Теперь при очистке поля будут логи в консоли
```

### Проверка 3: Структура сниппетов
```javascript
// Показать все сниппеты с подсветкой:
document.querySelectorAll('.context-snippet').forEach((sn, i) => {
    var hl = sn.querySelectorAll('.highlight');
    if (hl.length > 0) {
        console.log(`Snippet ${i}:`, hl.length, 'highlights', sn.innerHTML.substring(0, 100));
    }
});
```

---

## ✅ Критерии успеха

Все тесты должны пройти:
- [ ] Тест 1: При загрузке нет подсветки (0)
- [ ] Тест 2: При поиске подсветка появляется (> 0)
- [ ] Тест 3: При очистке поля подсветка исчезает (0)
- [ ] Тест 4: При новом вводе старая подсветка удаляется (0)
- [ ] Тест 5: Функция clearHighlights работает вручную
- [ ] Тест 6: После перезагрузки подсветки нет (0)

---

## 📝 Как сообщить о результатах

Если тесты НЕ проходят, укажите:
1. Номер теста который провалился
2. Ожидаемый результат vs фактический
3. Скриншот консоли (если возможно)
4. Текст ошибки (если есть)

Пример:
```
❌ Тест 3 провалился
Ожидалось: 0
Фактически: 8
Консоль: document.querySelectorAll('.highlight').length вернул 8
Вывод: При очистке поля подсветка не исчезает
```
