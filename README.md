# ZAKUPKI_WEB - Пакетный анализ документов

## 📋 Описание
Веб-приложение для загрузки и поиска по ключевым словам в документах различных форматов. Предназначено для автоматизации обработки документов.

## ✨ Функциональность

### 📁 Загрузка файлов

- **Ключевые слова**: Поиск по одному или нескольким терминам (через запятую)
- **Сниппеты**: Контекстные фрагменты с подсветкой найденных слов
- **Удаление**: Кнопка "Удалить все" для полной очистки загруженных файлов и индекса
- **Навигация**: Сворачивание/разворачивание папок

### 🛠️ Дополнительно
## 🚀 Быстрый старт

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск приложения
python app.py
```

Приложение будет доступно по адресу: http://127.0.0.1:8081

## 📦 Структура проекта

```
ZAKUPKI_WEB/
├── app.py                 # Основное Flask приложение
├── requirements.txt       # Зависимости Python
├── webapp/               # Модульная структура приложения
│   ├── __init__.py       # Фабрика приложения
│   ├── config.py         # Конфигурация
│   ├── routes/           # Роуты (blueprints)
│   └── services/         # Бизнес-логика
│   └── index.html        # Главная страница
├── static/              # Статические файлы
│   ├── css/styles.css   # Стили CSS
│   └── js/script.js     # JavaScript логика
├── uploads/             # Папка для загруженных файлов
├── index/               # Папка для индекса и метаданных
└── logs/                # Логи приложения
```

## 🔧 Конфигурация
- **Максимальный размер файла**: 100MB
- **Папка загрузок**: uploads/
- **Папка индекса**: index/

### Автоматический Garbage Collection (APScheduler)

Планировщик автоматически запускается при старте приложения и выполняет GC ежедневно в заданное время. Управление через административную панель: `/admin` → секция "⏰ Автоматический Garbage Collection".

### Поддерживаемые форматы
- **PDF**: Извлечение текста с помощью pypdf и pdfplumber
- **DOCX**: Обработка через python-docx
- **XLS/XLSX**: Чтение через openpyxl
- **TXT**: Поддержка различных кодировок (UTF-8, CP1251, Latin-1)
## 🔒 Безопасность
- Проверка расширений файлов

## � API ключи для AI-моделей

Начиная с версии 2.1.0, API ключи для LLM-моделей хранятся в PostgreSQL с Fernet шифрованием.

### Настройка

1. **Переменная окружения** в `.env`:
   ```bash
   ```

### Управление ключами

- `DELETE /api_keys/delete_by_id/<id>` — удалить ключ

  -H "Authorization: Bearer <JWT_TOKEN>" \
  -H "Content-Type: application/json" \

### Безопасность
- Все ключи шифруются Fernet перед сохранением в БД
- Поддержка персональных и общих (shared) ключей
- JWT аутентификация для всех операций
- Изоляция ключей между пользователями

**Подробности:** см. `increments_doc/013_increment_2025-11-03.md`

## �🐛 Логирование и отладка
Логи пишутся в файл `logs/app.log` с ежедневной ротацией. В журнал попадают:
- Загрузка и удаление файлов
- Процесс индексации и поиска
- Ошибки обработки
- HTTP-запросы с временем выполнения

## 📝 Версия
v2.2.0 - Индексация документов в PostgreSQL с инкрементальными обновлениями и GC

## 🗄️ Переход на PostgreSQL (increment 013)
- Подробности: `increments_doc/013_increment_2025-11-03.md`
- Скрипт: `scripts/migrate_files_to_db.py`

Минимальные шаги:
- Установите переменные окружения `DATABASE_URL` и `API_ENCRYPTION_KEY` (Fernet)
- Выполните пробный запуск: `.../python scripts/migrate_files_to_db.py --dry-run`
- Выполните миграцию: `.../python scripts/migrate_files_to_db.py`

Примечание: для семантического поиска требуется расширение PostgreSQL `pgvector`.

### Требования окружения

- Python 3.9+
- PostgreSQL 14+ (рекомендуется) с расширением `pgvector`
- Опционально для расширенной индексации:
  - Tesseract OCR (пакет `tesseract`) и `pytesseract`/`Pillow`
  - `unrar` (для RAR-архивов) и пакет `rarfile`

Переменные окружения (минимум):

- `DATABASE_URL` — строка подключения к PostgreSQL, например:

  ```bash
  export DATABASE_URL="postgresql://user:pass@localhost:5432/zakupki"
  ```

- `FERNET_ENCRYPTION_KEY` — ключ шифрования для хранения API-ключей (см. раздел про API ключи).

Опциональные:

- `FLASK_HOST` (по умолчанию 127.0.0.1), `FLASK_PORT` (по умолчанию 8081), `FLASK_DEBUG` (True/False)

## 🗂️ Архитектура индексации в БД (increment 015)

- **Дедупликация**: При повторной загрузке файла с тем же SHA256 переиспользуются существующие чанки
- **Мягкое удаление**: Файлы помечаются как `is_visible=FALSE` и могут быть восстановлены без переиндексации
- **Garbage Collection**: Автоматическая очистка неиспользуемых документов на основе retention score
- **Административная панель**: Управление хранилищем, квотами, просмотр статистики и логов аудита

### Структура данных

**Таблица `documents`:**
- `last_accessed_at`, `access_count` — метрики использования
- `indexing_cost_seconds` — стоимость обработки

**Таблица `chunks`:**
- `document_id`, `owner_id`, `chunk_idx`, `text`, `tokens`
- `text_sha256` — для дедупликации чанков
- `embedding` — опциональные векторные эмбеддинги

**Таблица `folder_index_status`:**
- `owner_id`, `folder_path`, `root_hash`, `last_indexed_at`
- Используется для инкрементальных обновлений

### Работа с индексом

**Построение индекса:**
```bash
POST /build_index
# Сканирует папку uploads/, вычисляет root_hash
# Индексирует только изменённые файлы
# Сохраняет чанки (500 токенов, перекрытие 50) в БД
```

**Поиск:**
```bash
POST /search
# Полнотекстовый поиск по таблице chunks
# Использует PostgreSQL FTS (to_tsvector) с GIN индексом
# Фильтрация по owner_id, возврат сниппетов
```

**Дедупликация при загрузке:**
- Новый файл → индексируется обычно
- Существующий видимый → пропускается (используются существующие чанки)
- Мягко удалённый → восстанавливается (`is_visible=TRUE`)
- Файл другого пользователя → создаётся новая запись с копированием чанков

### Garbage Collection

**Формула retention score:**
```
score = 0.5 × ln(access_count + 1) - 0.3 × days_since_access + 0.2 × indexing_cost_minutes
```

Документы с `score < threshold` (по умолчанию -10.0) считаются кандидатами на удаление.

**API административной панели:**
- `GET /admin/storage` — страница управления
- `GET /admin/stats` — статистика хранилища
- `GET /admin/gc/candidates` — кандидаты на удаление
- `POST /admin/gc/run` — запуск GC (с dry-run режимом)
- `POST /admin/uploads/toggle` — блокировка загрузок
- `GET /admin/audit_log` — история операций

Доступ к панели только для пользователей с ролью `admin`.

### Миграция с файлового индекса

Legacy файловый индекс `_search_index.txt` больше не поддерживается. Все данные теперь хранятся в PostgreSQL.

**Конфигурация** (убрана из `config_service.py`):
- ~~`USE_DB_INDEX`~~ — удалён, всегда используется БД
- Добавлены: `CHUNK_SIZE_TOKENS=500`, `CHUNK_OVERLAP_TOKENS=50`, `USER_QUOTA_GB=10`

**Миграция БД:**
```bash
# Применить миграцию
psql $DATABASE_URL < migrations/015_db_indexing_lifecycle.sql
```

Подробнее см. `increments_doc/015-increment-CLEAN.md`

## 🧹 Отказ от файлового индекса

С инкрементом 015 система окончательно перешла на индексацию в PostgreSQL. Файловый индекс `_search_index.txt` не используется и не создаётся (за исключением исторических тестов). Статус прогресса групп (если включён) сохраняется во временный файл `index/status.json` как телеметрия UI.

Для построения индекса используйте `POST /build_index` (индексирует `uploads/` в БД). Поиск выполняется только по данным БД через `POST /search`.