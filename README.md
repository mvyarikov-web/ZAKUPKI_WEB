# ZAKUPKI_WEB - Пакетный анализ документов

## 📋 Описание
Веб-приложение для загрузки и поиска по ключевым словам в документах различных форматов. Предназначено для автоматизации обработки документов.

## ✨ Функциональность

### 📁 Загрузка файлов

- **Ключевые слова**: Поиск по одному или нескольким терминам (через запятую)
- **Сниппеты**: Контекстные фрагменты с подсветкой найденных слов
- **Удаление**: Кнопка "Удалить все" для полной очистки загруженных файлов и индекса
- **Навигация**: Сворачивание/разворачивание папок

### 🛠️ Дополнительно
## 🚀 Быстрый старт

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск приложения
python app.py
```

Приложение будет доступно по адресу: http://127.0.0.1:8081

## 📦 Структура проекта

```
ZAKUPKI_WEB/
├── app.py                 # Основное Flask приложение
├── requirements.txt       # Зависимости Python
├── webapp/               # Модульная структура приложения
│   ├── __init__.py       # Фабрика приложения
│   ├── config.py         # Конфигурация
│   ├── routes/           # Роуты (blueprints)
│   └── services/         # Бизнес-логика
│   └── index.html        # Главная страница
├── static/              # Статические файлы
│   ├── css/styles.css   # Стили CSS
│   └── js/script.js     # JavaScript логика
├── uploads/             # Папка для загруженных файлов
├── index/               # Папка для индекса и метаданных
└── logs/                # Логи приложения
```

## 🔧 Конфигурация
- **Максимальный размер файла**: 100MB
- **Папка загрузок**: uploads/
- **Папка индекса**: index/

### Автоматический Garbage Collection (APScheduler)

Планировщик автоматически запускается при старте приложения и выполняет GC ежедневно в заданное время. Управление через административную панель: `/admin` → секция "⏰ Автоматический Garbage Collection".

### Поддерживаемые форматы
- **PDF**: Извлечение текста с помощью pypdf и pdfplumber
- **DOCX**: Обработка через python-docx
- **XLS/XLSX**: Чтение через openpyxl
- **TXT**: Поддержка различных кодировок (UTF-8, CP1251, Latin-1)
## 🔒 Безопасность
- Проверка расширений файлов

## � API ключи для AI-моделей

Начиная с версии 2.1.0, API ключи для LLM-моделей хранятся в PostgreSQL с Fernet шифрованием.

### Настройка

1. **Переменная окружения** в `.env`:
   ```bash
   ```

### Управление ключами

- `DELETE /api_keys/delete_by_id/<id>` — удалить ключ

  -H "Authorization: Bearer <JWT_TOKEN>" \
  -H "Content-Type: application/json" \

### Безопасность
- Все ключи шифруются Fernet перед сохранением в БД
- Поддержка персональных и общих (shared) ключей
- JWT аутентификация для всех операций
- Изоляция ключей между пользователями

**Подробности:** см. `increments_doc/013_increment_2025-11-03.md`

## �🐛 Логирование и отладка
Логи пишутся в файл `logs/app.log` с ежедневной ротацией. В журнал попадают:
- Загрузка и удаление файлов
- Процесс индексации и поиска
- Ошибки обработки
- HTTP-запросы с временем выполнения

## 📝 Версия
v2.2.0 - Индексация документов в PostgreSQL с инкрементальными обновлениями и GC

## 🗄️ Переход на PostgreSQL (increment 013)
- Подробности: `increments_doc/013_increment_2025-11-03.md`
- Скрипт: `scripts/migrate_files_to_db.py`

Минимальные шаги:
- Установите переменные окружения `DATABASE_URL` и `API_ENCRYPTION_KEY` (Fernet)
- Выполните пробный запуск: `.../python scripts/migrate_files_to_db.py --dry-run`
- Выполните миграцию: `.../python scripts/migrate_files_to_db.py`

Примечание: для семантического поиска требуется расширение PostgreSQL `pgvector`.

### Требования окружения

- Python 3.9+
- PostgreSQL 14+ (рекомендуется) с расширением `pgvector`
- Опционально для расширенной индексации:
  - Tesseract OCR (пакет `tesseract`) и `pytesseract`/`Pillow`
  - `unrar` (для RAR-архивов) и пакет `rarfile`

Переменные окружения (минимум):

- `DATABASE_URL` — строка подключения к PostgreSQL, например:

  ```bash
  export DATABASE_URL="postgresql://user:pass@localhost:5432/zakupki"
  ```

- `FERNET_ENCRYPTION_KEY` — ключ шифрования для хранения API-ключей (см. раздел про API ключи).

Опциональные:

- `FLASK_HOST` (по умолчанию 127.0.0.1), `FLASK_PORT` (по умолчанию 8081), `FLASK_DEBUG` (True/False)

## 🗂️ Архитектура индексации в БД (increment 015)

### 🗄️ Модель хранения данных (гибридная архитектура)

Проект использует **гибридную модель** хранения:

**Физические файлы** → `uploads/` на диске
- Исходные файлы сохраняются в папку `uploads/` при загрузке
- Необходимы для извлечения текста (`extract_text()` парсит PDF/DOCX/XLS)
- Используются для скачивания (`/download/<path>`)
- Удаляются физически только при запуске Garbage Collection

**Метаданные и индекс** → PostgreSQL
- `documents` — метаданные файлов (SHA256, размер, метрики)
- `user_documents` — связь пользователей с документами (многие-ко-многим)
- `chunks` — текстовые фрагменты для поиска (500 токенов, перекрытие 50)
- `search_index` — полнотекстовый индекс PostgreSQL (tsvector, GIN)

**Поток данных:**
```
1. Загрузка:  file.save() → uploads/file.pdf
2. Хеширование: SHA256 для дедупликации
3. Извлечение: extract_text(uploads/file.pdf) → текст
4. Чанкирование: текст → chunks (500 токенов)
5. Индексация: chunks → search_index (tsvector)
6. Поиск: keywords → search_index → результаты (из БД!)
```

**Важно**: Поиск выполняется **ТОЛЬКО по БД**, физические файлы НЕ читаются при поиске.

### Легаси-архитектура БД (глобальный пул документов)

**Таблица `documents`** (глобальная, между всеми пользователями):
- Уникальность по `sha256_hash` — один и тот же файл хранится ОДИН раз
- Дедупликация: повторная загрузка не создаёт дубликат
- Метрики: `access_count`, `last_accessed_at`, `indexing_cost_seconds`, `size_bytes`
- Владелец: связь через `user_documents` (многие-ко-многим)

**Таблица `user_documents`** (связь пользователей с документами):
- `user_id` → `document_id` — кто может видеть файл
- `is_soft_deleted` — мягкое удаление (файл скрыт, но не удалён физически)
- `is_visible` — видимость для пользователя

**Таблица `chunks`** (текстовые фрагменты):
- Принадлежат `document_id` (не owner_id!)
- Переиспользуются при дедупликации
- `chunk_idx`, `text`, `tokens`, `text_sha256`
- Опциональный `embedding` для векторного поиска

**Таблица `folder_index_status`**:
- `owner_id`, `folder_path`, `root_hash`, `last_indexed_at`
- Используется для инкрементальных обновлений

### Ключевые возможности

- **Дедупликация**: При повторной загрузке файла с тем же SHA256 переиспользуются существующие чанки
- **Мягкое удаление**: Файлы помечаются как `is_visible=FALSE` и могут быть восстановлены без переиндексации
- **Garbage Collection**: Автоматическая очистка неиспользуемых документов на основе retention score
- **Административная панель**: Управление хранилищем, квотами, просмотр статистики и логов аудита

### 📊 Стоимость хранения и метрики документов

Каждый документ в системе имеет набор метрик для расчёта ценности:

**Метрики в таблице `documents`:**
```sql
access_count INTEGER DEFAULT 0           -- Количество обращений к файлу
indexing_cost_seconds FLOAT DEFAULT 0    -- Стоимость индексации в секундах
last_accessed_at TIMESTAMP               -- Последнее обращение (при поиске)
created_at TIMESTAMP                     -- Дата создания записи
size_bytes INTEGER                       -- Размер исходного файла
```

**Обновление метрик:**
- **При поиске**: `access_count++` и `last_accessed_at = NOW()`
- **При индексации**: `indexing_cost_seconds = время_обработки`
- **При просмотре**: `last_accessed_at = NOW()`

### 🗑️ Garbage Collection (автоматическая очистка)

**Retention Score** — оценка ценности документа для определения кандидатов на удаление.

**Формула для активных документов** (`is_visible=TRUE`):
```
score = 0.5 × ln(access_count + 1)           [+] Чем чаще используется
      - 0.3 × days_since_last_access         [-] Чем старее последний доступ
      + 0.2 × (indexing_cost_seconds / 60)   [+] Чем дороже была индексация
```

**Специальные случаи для мягко удалённых** (`is_visible=FALSE`):
```python
if days_since_deletion > 30:
    score = -100.0  # Удалить немедленно (физически из uploads/)
else:
    score = -50.0   # Низкий приоритет, ожидание
```

**Пороговые значения:**
- `score < -10.0` (по умолчанию) → кандидат на физическое удаление
- `score < -50.0` → мягко удалён недавно
- `score < -100.0` → мягко удалён > 30 дней (критично удалить)

**Интерпретация:**
- **Положительный score**: Файл часто используется, высокая ценность
- **Около нуля**: Файл редко используется, средняя ценность
- **Отрицательный score**: Файл не используется, кандидат на удаление

**Пример:**
```python
# Файл с 100 обращениями, последний доступ 5 дней назад, индексация 120 сек
score = 0.5 × ln(101) - 0.3 × 5 + 0.2 × 2 = 2.3 - 1.5 + 0.4 = 1.2
# Результат: файл ценен, удалять НЕ нужно

# Файл с 1 обращением, последний доступ 60 дней назад, индексация 30 сек
score = 0.5 × ln(2) - 0.3 × 60 + 0.2 × 0.5 = 0.35 - 18 + 0.1 = -17.55
# Результат: кандидат на удаление (score < -10.0)
```

### Работа с индексом

**Построение индекса:**
```bash
POST /build_index
# Сканирует папку uploads/, вычисляет root_hash
# Индексирует только изменённые файлы
# Сохраняет чанки (500 токенов, перекрытие 50) в БД
```

**Поиск:**
```bash
POST /search
# Полнотекстовый поиск по таблице search_index
# Использует PostgreSQL FTS (tsvector) с GIN индексом
# Фильтрация по owner_id, возврат сниппетов из БД
```

**Дедупликация при загрузке:**
- Новый файл → индексируется обычно
- Существующий видимый → пропускается (используются существующие чанки)
- Мягко удалённый → восстанавливается (`is_visible=TRUE`)
- Файл другого пользователя → создаётся новая запись с копированием чанков

### Garbage Collection

**Формула retention score:**
```
score = 0.5 × ln(access_count + 1) - 0.3 × days_since_access + 0.2 × indexing_cost_minutes
```

Документы с `score < threshold` (по умолчанию -10.0) считаются кандидатами на удаление.

**API административной панели:**
- `GET /admin/storage` — страница управления хранилищем
- `GET /admin/stats` — статистика хранилища (всего/используется/удалено)
- `GET /admin/gc/candidates?threshold=-10.0` — список кандидатов на удаление
- `POST /admin/gc/run` — запуск GC (параметры: `threshold`, `max_deletions`, `dry_run`)
- `POST /admin/uploads/toggle` — блокировка/разблокировка загрузок
- `GET /admin/audit_log?limit=100` — история операций GC
- `POST /admin/gc/schedule` — настройка автопланировщика (час запуска, порог)

**Конфигурация GC через переменные окружения:**
```bash
GC_SCHEDULE_HOUR=3           # Час запуска (по умолчанию 3:00)
GC_THRESHOLD_SCORE=-10.0     # Порог для удаления
GC_MAX_DELETIONS=100         # Макс. кол-во файлов за раз
GC_ENABLED=true              # Автозапуск включён/выключен
```

Доступ к панели только для пользователей с ролью `admin`.

### Миграция с файлового индекса

Legacy файловый индекс `_search_index.txt` больше не поддерживается. Все данные теперь хранятся в PostgreSQL.

**Конфигурация** (убрана из `config_service.py`):
- ~~`USE_DB_INDEX`~~ — удалён, всегда используется БД
- Добавлены: `CHUNK_SIZE_TOKENS=500`, `CHUNK_OVERLAP_TOKENS=50`, `USER_QUOTA_GB=10`

**Миграция БД:**
```bash
# Применить миграцию
psql $DATABASE_URL < migrations/015_db_indexing_lifecycle.sql
```

Подробнее см. `increments_doc/015-increment-CLEAN.md`

## 🧹 Отказ от файлового индекса и переход на PostgreSQL

С инкрементом 015 система окончательно перешла на индексацию в PostgreSQL. 

**Что удалено:**
- ~~`_search_index.txt`~~ — файловый индекс больше НЕ создаётся и НЕ используется
- ~~`USE_DB_INDEX`~~ — флаг удалён из конфигурации, всегда используется БД
- Файловый поиск по `_search_index.txt` удалён из кода

**Что добавлено:**
- `search_index` таблица с tsvector и GIN индексом для полнотекстового поиска
- Автоматический триггер `search_index_tsvector_trigger()` для обновления индекса
- Конфигурация чанкирования: `CHUNK_SIZE_TOKENS=500`, `CHUNK_OVERLAP_TOKENS=50`
- Квоты пользователей: `USER_QUOTA_GB=10` (по умолчанию)

**Телеметрия UI:**
- `index/status.json` — временный файл для отображения прогресса индексации в UI
- **Не используется для поиска**, только для визуализации

**Для построения индекса:**
```bash
POST /build_index  # Индексирует uploads/ в БД (search_index таблица)
```

**Для поиска:**
```bash
POST /search  # Поиск ТОЛЬКО по БД (search_index), не по файлам
```

Подробнее см. `increments_doc/015-increment-CLEAN.md`

---

## 🚧 Известные проблемы и рекомендации по рефакторингу

### ⚠️ Критический приоритет

1. **Промпты и результаты анализа сохраняются в файлы**
   - **Проблема**: `ai_analysis.py` сохраняет workspace, промпты и результаты в JSON файлы (`index/`)
   - **Риск**: Потеря данных при очистке, нет версионирования, нет многопользовательской изоляции
   - **Решение**: Мигрировать в таблицу `prompts` (уже есть) и создать `ai_analysis_results`

2. **Конфигурация моделей в JSON файле**
   - **Проблема**: `index/models.json` вместо таблицы `ai_model_configs`
   - **Риск**: Невозможность динамического обновления, отсутствие изоляции
   - **Решение**: Написать скрипт `scripts/migrate_models_json_to_db.py`

3. **Статус индексации в файле**
   - **Проблема**: `index/status.json` вместо таблицы `folder_index_status`
   - **Риск**: Race conditions при многопользовательской работе
   - **Решение**: Расширить `folder_index_status` полем `status_data JSONB`

### 🔶 Средний приоритет

4. **Дублирование кода поиска**
   - `_search_in_db()` и `_search_in_chunks()` дублируют логику формирования результатов
   - **Решение**: Вынести в `_format_search_results()` и `SnippetService`

5. **Архитектура легаси (глобальный пул документов)**
   - Трёхуровневая структура: `documents` → `user_documents` → `chunks`
   - **Вопрос**: Нужен ли sharing документов между пользователями?
   - **Если НЕТ**: Упростить до `documents(owner_id)` без `user_documents`
   - **Если ДА**: Добавить materialized view для ускорения запросов

6. **Магические числа в коде**
   - `chunk_size=500`, `overlap=50`, `threshold=-10.0` разбросаны по коду
   - **Решение**: Централизовать в `config_service.py` с поддержкой `.env`

### 🔹 Низкий приоритет (будущее)

7. **Полный переход на хранение файлов в БД**
   - Сейчас: файлы всегда в `uploads/`, поле `documents.blob` не используется
   - **Предложение для v3.0**: Файлы < 10MB → `blob`, остальные → S3/MinIO
   - **Выгода**: Единая точка хранения, атомарность, автоматический backup
   - **Риск**: Увеличение размера БД, замедление backup/restore